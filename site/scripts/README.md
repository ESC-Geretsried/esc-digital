# Website build and validation helpers

Deterministic build/validation scripts belong here.

Requirements:

- no embedded secrets;
- non-interactive CI execution;
- reproducible output from Git-tracked inputs plus explicitly documented non-secret parameters;
- validation must be usable without an AI provider.
